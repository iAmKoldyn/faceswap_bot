# FaceFusion Telegram Bot (FaceSwap)

Телеграм-бот, который ставит задачи в FaceFusion 3.5.2, обрабатывает фото/видео и отдает результат. Работает в личке и в группах (inline-кнопки), с раздельными очередями для фото (CPU) и видео (GPU).

## Быстрый старт
1. Установи зависимости:
   ```bash
   pip install -r requirements.txt
   ```
2. Создай `.env` в корне:
   ```env
   TELEGRAM_BOT_TOKEN=xxx
   # опционально
   FACEFUSION_VIDEO_EXEC=cuda
   FACEFUSION_IMAGE_EXEC=cpu
   FACEFUSION_DIR=./facefusion-3.5.2
   ```
3. Проверь пути в `facefusion-3.5.2/facefusion.ini` (раздел `[paths]`) — по умолчанию используются папки на уровень выше: `source_paths`, `target_path`, `output_path`, `jobs_path`, `temp_path`.
4. Запусти бота:
   ```bash
   python bot.py
   ```

## Как использовать
### В личке
1) `/start` → выбрать режим.  
2) Отправить Фото 1 (source).  
3) Отправить target (фото или видео).  
4) Для видео в личке бот спросит кадр (номер кадра = секунда * fps) или «Пропустить».  
5) Подтвердить запуск. Получить результат (фото или видео).

### В группе
- Обращайся через @ или по inline-кнопкам; после первого нажатия можно слать файлы без @ (пока активен сеанс).
- Режим выбирается inline-кнопками.
- Можно отправить фото+фото или фото+видео одним альбомом (media group) — бот сам распределит на source/target и сразу покажет подтверждение (кадр для видео в группах не спрашивается).

## Режимы (модели)
- 🎬 Фото → Видео (быстро): `inswapper_128_fp16` + `gfpgan_1.4`
- 🎥 Фото → Видео (качество): `hyperswap_1c_256` + `codeformer`
- 🖼️ Фото → Фото (gpen): `hyperswap_1c_256` + `gpen_bfr_1024`
- 🖼️ Фото → Фото (codeformer): `hyperswap_1c_256` + `codeformer`

## Ограничения
- Форматы: фото — jpg/jpeg/png; видео — mp4/mov.
- Видео: до 60 МБ и до 2 минут.
- Порядок: сначала всегда Фото 1, затем target (фото/видео по режиму).

## Очереди и ресурсы
- Две очереди: видео (GPU, `FACEFUSION_VIDEO_EXEC`, VMS из `FACEFUSION_VIDEO_VMS`) и фото (CPU, `FACEFUSION_IMAGE_EXEC`, VMS из `FACEFUSION_IMAGE_VMS`).
- FaceFusion запускается через CLI job-manager (`job-create` → `job-add-step` → `job-submit` → `job-run`).

## Полезные команды
- `/start` — приветствие, выбор режима.
- `/info` — краткая инструкция.
- `/mode <name>` — смена режима вручную.
- `/reset` — сброс текущей загрузки (отмена).

## Структура
- `bot.py` — логика бота, inline/reply UI, очереди, валидации.
- `facefusion-3.5.2/` — ванильная FaceFusion 3.5.2 с фиксами (в т.ч. лок по inference pool).
- Папки данных (игнорируются git): `jobs_path`, `source_paths`, `target_path`, `output_path`, `temp_path`, кэши моделей.

## Советы
- Перед первым запуском скачай модели FaceFusion: `python facefusion.py force-download --config-path facefusion.ini` (из `facefusion-3.5.2`).
- В группах большие видео могут дольше «висеть» на скачивании; бот пишет «Получаю видео…» сразу после приёма target.
- Если меняешь GPU/CPU — правь `.env` или `execution_providers` в ini, либо передавай переменные окружения.
